---

title: xPack Component Manager v{{releaseVersion}} released
seo_tile: Version {{releaseVersion}} released
description: Version v{{releaseVersion}} is a TODO new/maintenance release.
keywords:
  - xpack
  - xcdl
  - release

date: {{releaseDate}}

authors: ilg-ul

# To be listed in the Releases page.
tags:
  - releases

# ----- Custom properties -----------------------------------------------------

version: "{{releaseVersion}}"

release_tag_url: "https://github.com/xpack/xcdl-cli-ts/tree/v{{releaseVersion}}"

---

import {PageMetadata} from '@docusaurus/theme-common';
import Image from '@theme/IdealImage';
import CodeBlock from '@theme/CodeBlock';
import Link from '@docusaurus/Link';

import Prerequisites from './_common/_prerequisites.mdx'
import DownloadAnalytics from './_common/_download-analytics.mdx'

TODO: select one summary

Version **{{releaseVersion}}** is a maintenance release; it fixes ...

Version **{{releaseVersion}}** is a new release; it adds ....

<!-- truncate -->

<PageMetadata title={frontMatter.seo_title} />

The [xPack Component Manager](https://xpack.github.io/xcdl-cli-ts/)
project is a TypeScript and dual mode CommonJS/ECMAScript Node.js module.

<Prerequisites nodeVersion="18.0.0"/>

## Install

For installation instructions, please refer to the
[Install Guide](/docs/install/)
page.

## Import

To include this module in TypeScript and ECMAScript modules, use `import`:

```typescript
import { Xxx } from 'xcdl'
```

In JavaScript with CommonJS, use `require()`:

```javascript
const { Xxx } = request('xcdl')
```

## Source code

- <Link to={frontMatter.release_tag_url}>GitHub tag v{frontMatter.version} tree</Link>

## Bug fixes

- none

## Enhancements

- none

## Known problems

- none

## CI tests

Before publishing, a set of tests were performed .
The results are available from:

- [GitHub Actions](https://github.com/xpack/xcdl-cli-ts/actions)

