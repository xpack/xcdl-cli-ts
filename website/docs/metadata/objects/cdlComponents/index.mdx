---
title: cdlComponents
description: The definition of the xCDL cdlComponents objects.
keywords:
  - xpack
  - xcdl
  - cdl
  - component
  - cdlComponents

date: 2015-11-15 12:00:00 +0200
---

import MetadataObjectLink from "@site/src/components/MetadataObjectLink";
import MetadataObjectPropertyLink from "@site/src/components/MetadataObjectPropertyLink";

import PropertyActiveIf from "../../_components/_active-if";
import PropertyCategory from "../../_components/_category";
import PropertyPrivateIncludeFolders from "../../_components/_private-include-folders";
import PropertyPrivateCompilerOptions from "../../_components/_private-compiler-options";
import PropertyPrivateDefines from "../../_components/_private-defines";
import PropertyPublicIncludeFolders from "../../_components/_public-include-folders";
import PropertyPublicCompilerOptions from "../../_components/_public-compiler-options";
import PropertyPublicDefines from "../../_components/_public-defines";
import PropertySourceFiles from "../../_components/_source-files";
import PropertyComputed from "../../_components/_computed";
import PropertyConfigurable from "../../_components/_configurable";
import PropertyDefaultEnabled from "../../_components/_default-enabled";
import PropertyDefaultValue from "../../_components/_default-value";
import PropertyDescription from "../../_components/_description";
import PropertyDisplay from "../../_components/_display";
import PropertyDocsUrl from "../../_components/_documentation-urls";
import PropertyCustomDefine from "../../_components/_custom-define";
import PropertyHeaderFile from "../../_components/_header-file";
import PropertyImplements from "../../_components/_implements";
import PropertyincludeCDLs from "../../_components/_include-xcdls";
import PropertyLegalValues from "../../_components/_legal-values";
import PropertyRequires from "../../_components/_requires";
import PropertyValueFormat from "../../_components/_value-format";
import PropertyValueType from "../../_components/_value-type";

{/* ------------------------------------------------------------------------ */}

# `cdlComponents` object

:::note

Work in progress.

:::

## Purpose

Define an array of component objects (generic units of functionality).

## Syntax

```json
{
  "cdlComponents": [
    {
      "id": "<component name>",
      "display": "<short name>",
      "description": "<long text>",

      ... component members ...
    },
    ...
  ]
}
```

The value of the `cdlComponents` property is an array of component objects, 
each having a name and a body. Names are map keys and must be unique within 
a parent. 

## Description

`cdlComponents` objects are configuration objects that can contain additional
options and subcomponents. A `cdlComponents` object can have the same properties
as an `cdlOptions` object. Additionally, the `includeCDLs` property enables the
configuration definitions to be distributed across multiple files and included
below the current object in the configuration hierarchy.

Each `cdlComponents` object is defined as a JSON object that has a name and a
body. Names are map keys and must be unique within a parent.

Similarly to `cdlOptions` objects, if active and enabled, `cdlComponents`
objects can generate preprocessor `#define` lines in a configuration header file
and decide if a set of source files are included in the build process
(if the `sourceFiles` property is also defined).

## Containers

The `cdlComponents` objects are inherently containers, allowing them to include
`cdlComponents`, `cdlOptions`, and `cdlInterfaces` that should be organized
below this object in the configuration hierarchy. These objects can be defined
either as embedded children or read from included files.

## Properties

- <PropertyActiveIf objectType="component" />
- <PropertyComputed objectType="component" />
- <PropertyConfigurable objectType="component" />
- <PropertyCustomDefine objectType="component" />
- <PropertyDefaultEnabled objectType="component" />
- <PropertyDefaultValue objectType="component" />
- <PropertyDescription objectType="component" />
- <PropertyDisplay objectType="component" />
- <PropertyDocsUrl objectType="component" />
- <PropertyHeaderFile objectType="component" />
- <PropertyImplements objectType="component" />
- <PropertyincludeCDLs objectType="component" />
- <PropertyLegalValues objectType="component" />
- <PropertyPrivateCompilerOptions objectType="component" />
- <PropertyPrivateDefines objectType="component" />
- <PropertyPrivateIncludeFolders objectType="component" />
- <PropertyPublicCompilerOptions objectType="component" />
- <PropertyPublicDefines objectType="component" />
- <PropertyPublicIncludeFolders objectType="component" />
- <PropertyRequires objectType="component" />
- <PropertySourceFiles objectType="component" />
- <PropertyValueFormat objectType="component" />
- <PropertyValueType objectType="component" />

## Example

A simple component, enabled by default.

```json
{
  "cdlComponents": [
    {
      "id": "useAsserts",
      "display": "Use asserts",
      "description": "If this component is enabled, assertions in the code are tested at run-time. Assert functions (OS_ASSERT()) are defined in `include/os/infra/os_ass.h` within the install tree. If the component is disabled, these result in no additional object code and no checking of the asserted conditions.",
      "includeCDLs": ["assert.json"]
    }
  ]
}
```

## See also

- <MetadataObjectLink name="cdlInterfaces" />
- <MetadataObjectLink name="cdlOptions" />

## eCos reference

- [`cdl-component`](https://ecos.sourceware.org/docs-latest/cdl-guide/ref.cdl-component.html)
