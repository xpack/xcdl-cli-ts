---

title: cdlPackage
description: The definition of the xCDL cdlPackage objects
keywords:
  - xpack
  - xcdl
  - cdl
  - package

date: 2015-11-15 12:00:00 +0200

---

import MetadataObjectLink from '@site/src/components/MetadataObjectLink';
import MetadataPropertyLink from '@site/src/components/MetadataPropertyLink';

import PropertyActiveIf from '../../_components/_active-if';
import PropertyCategory from '../../_components/_category';
import PropertyChildrenGeneratedFile from '../../_components/_children-generated-file';
import PropertyCompilerIncludeFolders from '../../_components/_compiler-include-folders';
import PropertyCompilerSourceFiles from '../../_components/_compiler-source-files';
import PropertyDefaultEnabled from '../../_components/_default-enabled';
import PropertyDefaultValue from '../../_components/_default-value';
import PropertyDescription from '../../_components/_description';
import PropertyDisplay from '../../_components/_display';
import PropertyDocsUrl from '../../_components/_docs-url';
import PropertyGeneratedDefinition from '../../_components/_generated-definition';
import PropertyGeneratedFile from '../../_components/_generated-file';
import PropertyImplements from '../../_components/_implements';
import PropertyincludeCDLs from '../../_components/_include-xcdls';
import PropertyParent from '../../_components/_parent';
import PropertyRequires from '../../_components/_requires';

{/* ------------------------------------------------------------------------ */}

# The `cdlPackage` definition

:::note

Work in progress.

:::

## Purpose

Define a package (a specialized component that can be distributed independently).

## Syntax

```json
{
    "cdlPackage": {
        "name": "<name>",
        "display": "<short name>",
        "description": "<long text>",

        ... package members ...
    }
}
```

## Description

The `cdlPackage` object is a unit of software distribution. It is also a configuration option in that users can choose whether or not a particular package is loaded into the configuration. It is also a component in that it can contain additional components and options in a hierarchy.

A `cdlPackage` can have most of the properties and behaviour of a `cdlComponents` object, but with some limitations (see the Unavailable properties below).

The top-level xCDL file for a package project must begin with a `cdlPackage` object.

## Containers

The `cdlPackage` is inherently a container, so it may include `cdlComponents`, `cdlOptions`, `cdlInterfaces` objects that will go below this object in the configuration hierarchy.

## Properties

- <PropertyActiveIf objectType="package"/>
- <PropertyChildrenGeneratedFile objectType="package"/>
- <PropertyCompilerIncludeFolders objectType="package"/>
- <PropertyCompilerSourceFiles objectType="package"/>
- <PropertyDefaultEnabled objectType="package"/>
- <PropertyDefaultValue objectType="package"/>
- <PropertyDescription objectType="component"/>
- <PropertyDisplay objectType="component"/>
- <PropertyDocsUrl objectType="component"/>
- <PropertyGeneratedDefinition objectType="package"/>
- <PropertyGeneratedFile objectType="package"/>
- <PropertyImplements objectType="package"/>
- <PropertyincludeCDLs objectType="package"/>
- `name` - the internal name/key used to identify the package
- <PropertyParent objectType="package"/>
- <PropertyRequires objectType="package"/>

## Unavailable properties

- <MetadataPropertyLink name="computed" /> - always false; the package's value cannot be computed
- <MetadataPropertyLink name="configurable" /> - always true; packages can always be enabled/disabled by the user during the configuration process
- <MetadataPropertyLink name="valueType" /> - always `bool`; packages do not have an associated value, they return 1 if enabled
- <MetadataPropertyLink name="valueFormat" /> - if the header definition is generated, the value will always be `(1)` [TODO: perhaps booleans can be formatted as true/false]
- <MetadataPropertyLink name="legalValues" /> - no restrictions can apply to package values

## Example

```json
{
    "cdlPackage": {
        "name": "infra",
        "display" : "Infrastructure",
        "description": "Common types and useful macros. Tracing and assertion facilities. Package startup options.",
        "generatedFile": "os/infra/infra.h",
        "compilerSourceFiles": [
            "startup.cpp",
            "prestart.cpp",
            "pkgstart.cpp",
            "userstart.cpp"
        ]
    }
}
```

## See also

- <MetadataObjectLink name="cdlComponents" />
- <MetadataObjectLink name="cdlInterfaces" />
- <MetadataObjectLink name="cdlOptions" />

## eCos reference

- [`cdl-package`](https://ecos.sourceware.org/docs-latest/cdl-guide/ref.cdl-package.html)
